Place optional file-association icons here.

Supported files:
- `build/file-associations/md.ico` for `.md`
- `build/file-associations/markdown.ico` for `.markdown`

If a file is missing, the build will fall back to the app icon for that extension.
